# Postmortem

系统性事故的机制分析与护栏闭环。模板：[`TEMPLATE.md`](TEMPLATE.md)。

## 何时写

机制不显然、漏网因测试/约定缺口、再发现成本高的**系统性**问题。单点笔误修完即止的不写。

**Agent 主动：** 判定属于上类时，在修复同变更内主动落盘 postmortem 并更新「事故 → 门」，不等用户点名。长期否决进 Agent Note，不要用 Note 代替本目录。

写完必须：

1. 按 `NNNN-slug.md` 编号落盘
2. 更新下表「事故 → 门」
3. 若本仓有 testing 专篇，同步事故→门表（`{{TESTING_DOC}}`）
4. 新常驻规则：根 AGENTS 只留摘要，正文进 `docs/` 专篇或 Note

## 事故 → 门

| 事故类 | 护栏 |
|---|---|
{{INCIDENT_GATE_ROWS}}

新系统性事故必须加行。
